"""
⚠️ DEPRECATED — 06_dataset.py (旧单头数据集)

这个文件是 Trenzition v1/v2 架构的旧数据集，仅包含 pKd 标签，没有 kcat/Km/辅因子/测量类型。
已被 `model/train.py` 中的 `OxidoreductaseDataset` 取代（三头模式）。

如需使用请从 model/train.py 导入 OxidoreductaseDataset。
"""
